---
version: '3.4'
services:
  benchmark-ann:
    build: 
      context: ./
      dockerfile: Dockerfile-ann
    volumes:
      - $PWD/benchmark-data:/var/hdf5
      - $PWD/results:/var/lib/benchmark/results
  weaviate:
    command:
    - --host
    - 0.0.0.0
    - --port
    - '8080'
    - --scheme
    - http
    image: docker.io/semitechnologies/weaviate:latest@sha256:819b214da876bc160e4c12063a1c99f23bca24f701431adc126c1fe83f3cbc86
    ports:
      - 8080:8080
    volumes:
      - $PWD/weaviate-data:/var/lib/weaviate
    restart: on-failure:0
    environment:
      QUERY_DEFAULTS_LIMIT: 25
      AUTHENTICATION_ANONYMOUS_ACCESS_ENABLED: 'true'
      PERSISTENCE_DATA_PATH: '/var/lib/weaviate'
      DEFAULT_VECTORIZER_MODULE: 'none'
      ENABLE_MODULES: ''
      CLUSTER_HOSTNAME: 'benchmark_node'
...
